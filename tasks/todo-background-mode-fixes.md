# TODO: Background-Mode Review Fixes

See [`plan-background-mode-fixes.md`](plan-background-mode-fixes.md) for full detail, dependency graph, and acceptance criteria. Spec: [`SPEC-background-mode-fixes.md`](../SPEC-background-mode-fixes.md).

## Wave 1 — independent
- [x] **Task 1** *(Model: Sonnet 5)* — **[Critical]** `gui.py`/`tests/test_throttle_history.py`: dropped `schedule=get_active_schedule()` from the `record_throttle_episode` call in `_dl_thread` (one line, `gui.py:2551-2552`). Agent also added `test_repeated_recompute_with_stable_signal_converges_not_compounds` to `tests/test_throttle_history.py`, but honestly flagged that it only exercises `VideoDownload.py` directly and therefore *cannot* detect a regression reintroduced at `gui.py`'s call site (it never imports/drives `gui.py` at all). *I closed that gap myself*: added `test_repeated_real_throttle_episodes_do_not_compound_the_schedule` to `tests/test_background_mode_controller.py`, driving the real `_dl_thread` against the real (non-mocked) `VideoDownload.record_throttle_episode`/`get_active_schedule`/`next_resume_at`. Falsifiability proven by hand: temporarily reintroduced the bug (`schedule=get_active_schedule()`), confirmed this new test fails (`[300,300,300,300]` vs. expected `[1440,5760,17280,34560]`), then reverted and confirmed it passes again.
- [x] **Task 2** *(Model: Sonnet 5)* — **[Suggestion]** `VideoDownload.py`/`tests/test_cookie_support.py`: added `_SUPPORTED_COOKIE_BROWSERS` frozenset (brave/chrome/chromium/edge/firefox/opera/safari/vivaldi/whale) and validation in `configure_cookies` (case-insensitive, rejects unknown values with a `log.warning`, never raises, never lets a bad value reach `_base_opts()`). 3 new tests. *(Verified independently: diff matches exactly, 9/9 cookie tests pass in isolation.)*
- [x] **▶ CHECKPOINT 1** — Full `pytest tests/ -q` green: **565 passed, 1 skipped** (564 from Tasks 1+2, +1 for the end-to-end regression test I added). Critical fix's reproduction re-verified by hand, including the falsifiability check the plan required.

## Wave 2 — depends on Task 1 (same file, sequenced)
- [x] **Task 3** *(Model: Sonnet 5)* — **[Important]** `gui.py`: `_maybe_resume_background` now normalizes both sides of the `songs_folder` comparison via `os.path.normcase(os.path.normpath(...))` before comparing, so a case or trailing-slash difference no longer silently blocks a resume. Logs added on the mismatch and unresumable-phase (`done`/unrecognized) branches; the "no state at all" branch was deliberately left silent (documented judgment call — it fires on every normal launch including the very first ever, so logging it would be pure noise, not signal; the two branches that DO log are the ones where something persisted but still didn't visibly resume, which is what a user troubleshooting "why didn't it resume" actually needs). *(4 new tests: case-difference match, trailing-slash-difference match, mismatch logs why, phase=done logs why. Verified independently: diff matches exactly, full suite re-run by hand — 569 passed, 1 skipped.)*
- [x] **▶ CHECKPOINT 2** — Full `pytest tests/ -q` green: **569 passed, 1 skipped**.

## Wave 3 — depends on Task 1 (extracts already-fixed code)
- [x] **Task 4** *(Model: Opus 4.8 — structural refactor of the highest-risk function in a fragile file)* — **[Suggestion]** `gui.py`: extracted `_dl_thread`'s background-mode logic into two new methods, `_handle_background_throttle(...)` (the persist-then-cancellable-wait on a throttle, returns `(throttle_count, episode_started_at, stopped)`) and `_resolve_background_episode(...)` (records + resets escalation on the next non-throttle outcome, no-op when nothing was in progress, returns `(episode_started_at, throttle_count)`). Every queue message, log line, and `background_state.json` write preserved in identical order — a pure mechanical move, not a rewrite. *(Verified independently: diff matches the pre-specified target shape line-for-line; full suite re-run by hand both before and after — 569 passed, 1 skipped, unchanged; confirmed no test file was touched by this task.)*

## Wave 4 — depends on Task 4 (same file, sequenced)
- [x] **Task 5** *(Model: Sonnet 5)* — **[Suggestion]** `gui.py`: added `self._background_badge_lbl` (empty by default, zero visual change when inactive) and a `_set_background_mode(active)` choke-point method that keeps `self._background_mode` and the badge text ("● Background" / "") in sync, replacing all 6 raw assignment sites (the `__init__` declaration itself is untouched, since it runs before the badge widget exists). Agent found and fixed a real issue with the literal spec'd snippet on its own: several pre-existing bare-instance tests build `App` via `object.__new__` without a full `_build_ui`, and `ctk.CTk`/`tk.Tk`'s own `__getattr__` recurses infinitely chasing a likewise-missing `self.tk` on such an instance — a plain `getattr(self, name, default)` doesn't catch a `RecursionError`. Fixed by looking the badge up via `self.__dict__.get(...)`, which bypasses `__getattr__` entirely; production behavior is unaffected since `_build_ui` always creates the badge first in the real app. *(3 new tests in `tests/test_background_mode_badge.py`. Verified independently: diff matches, all 6 call sites confirmed, full suite re-run by hand — 572 passed, 1 skipped.)*
- [x] **▶ CHECKPOINT 3 (final)** — Full `pytest tests/ -q` green: **572 passed, 1 skipped** (vs. 560/1 baseline at the start of this fix pass — +12 total new tests across all 5 tasks). Whole-diff review complete: every task's diff read and verified line-by-line against its design before being marked done, not accepted on any agent's summary alone. Critical fix independently re-verified twice: once via direct reproduction script (schedule collapses to `[300,300,300,300]` with the bug, stabilizes at `[1440,5760,17280,34560]` without it), and again via a genuine end-to-end regression test added during verification (`test_repeated_real_throttle_episodes_do_not_compound_the_schedule`) that was itself proven to fail against the reintroduced bug and pass against the fix.

---

### Notes
- Dependency graph: Tasks 1 & 2 are independent (different files) and can run in parallel. Task 3 is sequenced after Task 1 (same file, different region — file-conflict avoidance, not a true logical dependency). Task 4 has a **real** dependency on Task 1 (extracts code Task 1 already fixed). Task 5 is sequenced after Task 4 (same file).
- Every task verified independently (git diff + real test re-run) before being marked `[x]`, per this session's established practice — not accepted on an agent's own summary.
- Scope is exactly the 5 review findings — no scope growth, no touching `LONG_BACKOFF_SECONDS`/`_RECOMPUTE_THRESHOLD`/`_MIN_BACKOFF_SECONDS`/the recompute math itself, no changes to the non-background download path.
